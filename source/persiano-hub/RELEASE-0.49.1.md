# Batchly 0.49.1

- Replaces browser prompt/confirm dialogs for cash and e-transfer with in-app forms.
- Adds live cash change calculation and confirmation controls.
- Fixes POS search mode so the Available Now grid is truly hidden while displaying only matching results.
- Restores the tile grid when the search field is cleared.

Invoice email remains link-based; a PDF generator is not bundled in this release.
